# Todo — Ajouter un thème "Pride" dans la config

## Objectif
Ajouter un thème couleur **Pride** dans le sélecteur de thème de l'app Config
(section Appearance) + clé SD `system.color_theme`.

## Contexte
- Système de thème centralisé dans `src/core/Theme.cpp` (tableau `kPresets`).
- Le picker (`ConfigApp.cpp`) et la config sont **dynamiques** : ils itèrent
  `themeCount()`/`themeId()`/`themeLabel()`. Aucune liste codée en dur à mettre à jour.
- La palette est plate (couleurs uniques) → on répartit l'arc-en-ciel pride sur
  les rôles UI : texte=magenta, sélection=orange, dim=violet, success=vert,
  warning=jaune, error=rouge. Fond noir cohérent avec les thèmes sombres.

## Étapes
- [x] Ajouter le preset `pride` à `kPresets` dans `Theme.cpp`.
- [x] Mettre à jour le commentaire de `themeApply` dans `Theme.h` (liste des ids).
- [x] Mettre à jour `README.md` (liste des choix + commentaire JSON).
- [x] Bump `version.md` (0.1.13 → 0.1.14) + entrée `releases.md`.
- [x] Compiler (`pio run`).

## Review
- Modifs : `Theme.cpp` (1 preset), `Theme.h` (commentaire), `README.md` (2 endroits),
  `releases.md`, `version.md`.
- Validation : `pio run` (voir memory.md).
- Outcome : à compléter.
